/* charts.js - stub for inserting charts (use Chart.js / ApexCharts later) */
export function renderSparkline(el, values=[]){
  if(!el) return;
  el.innerHTML = '<div style="height:40px;display:flex;align-items:center;justify-content:center;color:#B1BFD6">Chart (stub)</div>';
}
